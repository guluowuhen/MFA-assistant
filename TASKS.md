# MFA助手 发布前任务清单（v0.8.x -> v1.0.0）

## 目标
- 当前阶段：内测（`0.8.0`）
- 下一里程碑：`0.9.0`（完成 P0）
- 正式发布：`1.0.0`（通过发布前 Checklist）

## P0（必须，建议 1 周内）

### 1. 导出增加“是否包含 secret”开关
- 目标：降低导出敏感信息的风险。
- 改动文件：
- `manage/manage.html`：导出前弹出选项（含/不含 `secret`）。
- `manage/manage.js`：`buildExportPayload` 支持参数控制是否包含 `secret`。
- 验收标准：
- 默认建议“不包含 `secret`”。
- 选择“包含 `secret`”时有明确风险提示。
- 导出 JSON 结构稳定，兼容现有导入逻辑。

### 2. 基础诊断信息（命中/未命中原因）
- 目标：提升问题排查效率，减少“看起来没生效”的反馈成本。
- 改动文件：
- `content/content.js`：补充命中判定结果与失败原因记录。
- `popup/popup.html`：新增“最近诊断信息”区域（简版）。
- `popup/popup.js`：读取并展示最近一次诊断结果。
- 验收标准：
- 至少能区分：未匹配 URL、匹配但缺少 secret、正常命中。
- 不展示完整 secret。

## P1（重要，建议 2-3 周）

### 3. URL 匹配策略升级（host/path 规则 + 优先级）
- 目标：降低同域多页面误匹配风险。
- 改动文件：
- `manage/manage.html`：每条绑定增加匹配策略配置项。
- `manage/manage.js`：保存规则（精确匹配/host+path 前缀等）。
- `content/content.js`：按策略优先级命中最合适系统。
- 验收标准：
- 同一域名多路径可稳定命中正确项。
- 旧数据无策略字段时按当前逻辑兼容。

### 4. 密钥管理增强（更换/更新时间/失效标记）
- 目标：让长期维护更可控。
- 改动文件：
- `manage/manage.html`：新增“更换密钥”入口与时间展示。
- `manage/manage.js`：维护元数据（更新时间、失效状态）。
- `PROJECT_CONTEXT.md`：补充数据结构与流程说明。
- 验收标准：
- 可单项更换密钥并即时生效。
- 可标记失效并在 popup/content 显示提醒。

### 5. 导入导出 schema 版本化与兼容
- 目标：为未来迭代保留兼容空间。
- 改动文件：
- `manage/manage.js`：导入导出 schema 升级与向后兼容。
- `PROJECT_CONTEXT.md`：新增 schema 版本说明。
- 验收标准：
- 至少兼容 v1（当前）与新版本。
- 不兼容文件给出明确报错。

## P2（优化项，正式发布后可继续）

### 6. 可选加密备份
- 目标：在导出包含 secret 时提升数据保护能力。
- 改动文件：
- `manage/manage.html`、`manage/manage.js`
- 验收标准：
- 支持密码加密导出与解密导入（错误密码有清晰提示）。

### 7. 本地操作审计（最近 N 次变更）
- 目标：提高可追溯性与回滚可行性。
- 改动文件：
- `manage/manage.js`、`popup/popup.js`
- 验收标准：
- 能查看最近变更记录（新增/删除/覆盖导入）。

### 8. UI 与性能细节优化
- 目标：提升大量配置时的交互体验。
- 改动文件：
- `manage/manage.css`、`manage/manage.js`、`popup/popup.css`
- 验收标准：
- 100+ 绑定项时操作流畅，无明显卡顿。

## 发布前 Checklist（v1.0.0 门槛）

### 功能门槛
- P0 全部完成并验收通过。
- P1 至少完成 3、4。

### 质量门槛
- 手测用例 >= 10 条（关键流程覆盖）。
- Chromium 环境验证 >= 3（Chrome/Edge/Arc）。
- 关键流程无阻断级问题（导入失败、命中失败、复制失败）。

### 文档门槛
- `PROJECT_CONTEXT.md` 与实际行为一致。
- `README.md` 补齐导入导出与诊断说明。
- 产出 `CHANGELOG`（本版本改动 + 已知风险 + 回滚方式）。

## 建议版本节奏
- `0.8.x`：当前内测基线。
- `0.9.0`：P0 完成，进入扩展内测。
- `0.9.5`：P1 核心完成并稳定一轮。
- `1.0.0`：通过 Checklist 后正式发布。
